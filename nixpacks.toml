[phases.setup]
nixPkgs = [
  "python311",
  "gcc",
  "zlib",
]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium",
  "python -m playwright install-deps chromium",
]

# [start] section intentionally removed — Procfile handles the start command
